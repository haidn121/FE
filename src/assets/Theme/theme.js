import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  spacing: 4,
  palette: {
    primary: {
      light: "#ffca28",
      main: "#ffb300",
      dark: "#ffb300",
      contrastText: "#fff",
    },
  },
});

export default theme;
